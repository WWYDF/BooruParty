// DON'T CHANGE THIS. This is what the dashboard updater uses.
// Manually changing this yourself could lead to accidental file deletion.

// Only developers should update this when pushing a new version.
export const CURRENT_VERSION = '1.0.0';